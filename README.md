# RepoCapsule
RepoCapsule is a Python library that streams GitHub or local repos into clean JSONL corpora for LLM pre‑training, fine‑tuning, or RAG. Includes structure‑aware chunking (code/Markdown), robust Unicode decoding, optional KQL‑from‑Markdown extraction, and an optional quality‑scoring pass.
