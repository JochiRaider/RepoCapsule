# sievio-accel

Optional Rust acceleration for Sievio QC hot paths.

Development install:

```bash
pip install -e ./accel
```

Or use maturin directly:

```bash
maturin develop --manifest-path accel/Cargo.toml
```
